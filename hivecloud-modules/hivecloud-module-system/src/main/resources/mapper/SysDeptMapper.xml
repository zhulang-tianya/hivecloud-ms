<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hivecloud.system.mapper.SysDeptMapper">

    <resultMap id="SysDeptResult" type="com.hivecloud.system.entity.SysDept">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="deptName" column="dept_name"/>
        <result property="sort" column="sort"/>
        <result property="leaderName" column="leader_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="selectDeptVo">
        SELECT id, tenant_id, parent_id, ancestors, dept_name, sort, leader_name, phone, email, status, create_time, update_time, deleted
        FROM sys_dept
    </sql>

    <select id="selectDeptTree" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        WHERE tenant_id = #{tenantId}
          AND deleted = 0
          AND status = 1
        ORDER BY sort ASC
    </select>

    <select id="selectChildDepts" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        WHERE tenant_id = #{tenantId}
          AND parent_id = #{parentId}
          AND deleted = 0
        ORDER BY sort ASC
    </select>
</mapper>
